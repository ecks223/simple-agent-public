# Fill in the API key(s) for the provider(s) you want to use.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# Optional: which memory strategy the FastAPI server uses at startup.
# One of: baseline, per_user, per_user_plus_patterns.
# Defaults to baseline (original starter behavior) if unset.
MEMORY_STRATEGY=
